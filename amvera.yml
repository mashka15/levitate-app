version: null
meta:
  environment: python
  toolchain:
    name: pip
    version: 3.13.7
build:
  requirementsPath: requirements.txt
run:
  scriptName: app.py
  command: sh -c "flask --app app db upgrade && flask run --host=0.0.0.0 --port=5000"

env:
  FLASK_APP: app
  FLASK_ENV: production  # Для продакшн-режима

