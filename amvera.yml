---
version: null
meta:
  environment: python
  toolchain:
    name: pip
    version: 3.13.7
build:
  requirementsPath: requirements.txt
run:
  scriptName: app.py
  persistenceMount: /data
  run:
    containerPort: 5000

  command: gunicorn --bind 0.0.0.0:5000 wsgi:app
