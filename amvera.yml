version: null
meta:
  environment: python
  toolchain:
    name: pip
    version: 3.13.7
build:
  requirementsPath: requirements.txt
run:
  run:
    scriptName: run.py  # Оставляем без изменений
    command: sh -c "flask db upgrade && python run.py"
env:
  FLASK_APP: app
  FLASK_ENV: production  # Для продакшн-режима


